# ===================================
# 🏢 Immo-App (Immobilier)
# immo-app.app.obotcall.tech → Port 3002
# ===================================

upstream immo_app_backend {
    server immo-app:3002;
    keepalive 32;
}

# HTTP → HTTPS redirect
server {
    listen 80;
    server_name immo-app.app.obotcall.tech;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$server_name$request_uri;
    }
}

# HTTPS
server {
    listen 443 ssl http2;
    server_name immo-app.app.obotcall.tech;

    # SSL Certificates
    ssl_certificate /etc/letsencrypt/live/immo-app.app.obotcall.tech/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/immo-app.app.obotcall.tech/privkey.pem;

    # SSL Configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;

    # Security Headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # Logs
    access_log /var/log/nginx/immo-app-access.log;
    error_log /var/log/nginx/immo-app-error.log;

    # Rate limiting
    limit_req zone=general burst=20 nodelay;

    location / {
        proxy_pass http://immo_app_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location /_next/static {
        proxy_pass http://immo_app_backend;
        proxy_cache_valid 200 60m;
        add_header Cache-Control "public, immutable";
    }
}
